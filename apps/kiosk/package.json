{
  "name": "@home-os/kiosk",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/main.cjs",
  "scripts": {
    "build": "echo 'Electron packaging lands in Phase 8' && exit 0",
    "dev": "echo 'Kiosk dev mode lands in Phase 8. Web UI is at http://localhost:5173'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no kiosk tests yet'"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
